# TKT-P8-004 Agent Report Status CLI

## Objective

Expose report generation and status tracking through the existing `agent` CLI group.

## Acceptance Criteria

- `pro-ai-server agent report` writes an implementation report.
- `pro-ai-server agent status` prints ticket counts and a status table.
- `--root` and `--phase` support isolated project and phase workflows.
