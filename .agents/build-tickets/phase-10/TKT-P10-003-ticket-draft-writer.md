# TKT-P10-003 Ticket Draft Writer

## Objective

Write generated ticket drafts safely.

## Acceptance Criteria

- Tickets are written under `.agents/build-tickets/{phase}/`.
- Existing tickets are not overwritten by default.
- `--force` allows intentional overwrite.
