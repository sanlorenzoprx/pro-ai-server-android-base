# TKT-P10-004 Agent Ticketize CLI

## Objective

Expose recommendation-to-ticket automation through the agent CLI group.

## Acceptance Criteria

- `agent ticketize` previews selected ticket drafts.
- `agent ticketize --write` writes selected ticket drafts.
- Missing review files return a clear non-zero CLI error.
