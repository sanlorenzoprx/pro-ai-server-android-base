# Contract: Context Quality

## Requirements

- Chunks carry `start_line` and `end_line`.
- Context headings use `path:start-end`.
- Re-indexing removes files no longer present.
- Existing search command remains stable.

